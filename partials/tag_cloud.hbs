{{!-- 标签云 --}}
<ul class="tag-cloud">
	{{#foreach tags}}
    	<a href="{{url}}" class="tag-item ar-tag-items color-radom">
			{{name}}
			<span class="post-count">{{post_count}}</span>
		</a>
	{{/foreach}}
</ul>